🛒 Listify (React + Firebase)

Listify is a simple and modern shopping list web app built with React.js and Firebase.
It lets users manage their shopping items effortlessly — add, edit, delete, and track everything in one place — all with real-time synchronization using Firebase Firestore.

📌 Overview

Listify is a CRUD-based web application that demonstrates how to combine React state management with Firebase Authentication and Firestore Database.

Users can:

- Log in with Google or Email.

- Add new shopping items.

- View all items in real-time.

- Edit or update existing items.

- Delete items instantly.

- Data stays synced automatically with Firebase Firestore.

🎯 Objectives

Build a complete CRUD app in React.

Integrate Firebase Authentication (Google + Email).

Connect and sync data with Firestore Database.

Structure React into modular components.

Deliver a clean, responsive UI with Bootstrap

| Category           | Technology                              |
| ------------------ | --------------------------------------- |
| **Frontend**       | React.js (Hooks, Components, JSX)       |
| **Backend / DB**   | Firebase Firestore (Serverless)         |
| **Authentication** | Firebase Auth (Google + Email/Password) |
| **Styling**        | Bootstrap               |  |


🛠 Features

✅ Authentication

- Login & Signup using Google or Email/Password.

- Each user has access to their own list (extendable).

✅ CRUD Operations

- Create: Add new items (with quantity, price, and category).

- Read: View all saved items in real-time.

- Update: Edit item details directly in the dashboard.

- Delete: Instantly remove items.

✅ Real-Time Sync

- Any update reflects instantly on screen using Firestore live updates.

✅ Responsive UI

- Clean design built with Bootstrap / Tailwind, optimized for all devices.

🔄 App Flow

- User opens Listify → sees homepage & About.

- Creates an account (Google or Email).

- Adds shopping items with quantity, price, and category.

- Edits or deletes items directly.

- All data syncs with Firestore in real time.

- User logs out → data stays saved securely.